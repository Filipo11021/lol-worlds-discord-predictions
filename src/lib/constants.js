import { config } from "../../config.js"

export const BASE_FOLDER = `data/${config.folderName}`
export const MESSAGES_PATH = `${BASE_FOLDER}/messages.json`
export const PREDICTIONS_PATH = `${BASE_FOLDER}/predictions.json`